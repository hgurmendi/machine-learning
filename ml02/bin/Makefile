all:
	gcc bp.c -o bp -lm
	gcc discretiza.c -o discretiza -lm
	gcc separa_clases.c -o separa_clases -lm
	gcc bp_weight_decay.c -o bp_weight_decay -lm

clean:
	rm -f bp discretiza separa_clases bp_weight_decay
